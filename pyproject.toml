[project]
name = "miedipole-py"
version = "0.1.0"
description = "Python port of MieDipole — Generalized Mie Theory with electric dipole source"
readme = "README_PYTHON.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.21,<2",
    "scipy>=1.7",
    "matplotlib>=3.5",
    "pandas>=1.3",
]

[tool.uv]
dev-dependencies = []
